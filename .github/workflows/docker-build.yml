# only run when we push changes to master or PRs
on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  run_straight_from_gpr:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: just some random command
        uses: docker://docker.pkg.github.com/sujaypillai/buildxdemo/buildxdemo:githubactions
  build_with_gpr:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: echo ${{ secrets.GITHUB_TOKEN }} | docker login docker.pkg.github.com -u $GITHUB_ACTOR --password-stdin
      - run: docker pull docker.pkg.github.com/sujaypillai/buildxdemo/buildxdemo:githubactions
